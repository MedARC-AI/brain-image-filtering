pip install numpy matplotlib tqdm scikit-image jupyterlab && pip install accelerate webdataset clip pandas matplotlib ftfy regex kornia umap-learn h5py && pip install torchvision==0.15.2 torch==2.0.1 && pip install diffusers && pip install deepspeed && python -m ipykernel install --user --name=fmri && pip install einops && pip install torchmetrics && pip install transformers && pip install ftfy regex tqdm && pip install git+https://github.com/openai/CLIP.git